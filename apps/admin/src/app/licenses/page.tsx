import AppShell from "@/components/layout/AppShell";
import LicensesFeature from "@/features/licenses";

export default function LicensesPage() {
  return (
    <AppShell>
      <LicensesFeature />
    </AppShell>
  );
}
